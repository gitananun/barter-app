import 'package:flutter/material.dart';

@immutable
class OnBoardingStyle {
  static const EdgeInsets contentPadding = EdgeInsets.symmetric(horizontal: 30, vertical: 10);
  static const EdgeInsets nextBtnIconPadding = EdgeInsets.all(10.0);
  static const double nextBtnIconSize = 35.0;
  static const double bottomAppBarHeight = 60;
  static const double topAppBarHeight = 60;
}
