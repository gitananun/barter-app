import 'package:flutter/material.dart';

class CustomThickDivider extends StatelessWidget {
  const CustomThickDivider({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) => const Divider(thickness: 5);
}
